# 🧪 دليل الاختبار والتشغيل

## ✅ قائمة التحقق قبل التجميع

### 1. البيئة
- [ ] Android Studio محدثة (الإصدار الأخير)
- [ ] Android SDK API 34 مثبت
- [ ] gradle wrapper موجود
- [ ] Java JDK موجود (تديره Android Studio)

### 2. الملفات
- [ ] activity_main.xml محدث (يحتوي على NavHostFragment)
- [ ] AndroidManifest.xml محدث (يحتوي على صلاحيات Android 13+)
- [ ] جميع Java files محدثة (بها فحوصات الأمان)

### 3. الاتصال
- [ ] الخادم يعمل أو متصل بـ Render.com
- [ ] URL الخادم صحيح في جميع Fragments

---

## 🚀 خطوات التجميع والتثبيت

### الخطوة 1: تحضير المشروع
```
1. افتح Android Studio
2. File → Open
3. اختر المجلد: c:\Users\DZ\Desktop\viddown\android
4. انتظر Gradle sync تنتهي
```

### الخطوة 2: مسح الملفات المؤقتة
```
1. Build → Clean Project (انتظر حتى ينتهي)
2. File → Invalidate Caches / Restart
3. اختر "Invalidate and Restart"
4. انتظر Android Studio تفتح من جديد
```

### الخطوة 3: إعادة بناء المشروع
```
1. Build → Rebuild Project
2. انتظر انتهاء التجميع
3. تحقق من عدم وجود أخطاء في "Build" tab
```

### الخطوة 4: تشغيل على محاكي أو جهاز
```
1. Run → Run 'app' أو اضغط Shift+F10
2. اختر الجهاز/المحاكي
   - إذا لم تر جهاز: اذهب إلى Tools → AVD Manager لإنشاء واحد
3. انتظر حتى يثبت التطبيق ويفتح
```

---

## 🧬 اختبارات الوحدات

### اختبار 1: الشاشة الرئيسية (Home)
```
✓ يفتح التطبيق بدون انهيار
✓ يظهر حقل البحث
✓ يظهر أزرار الجودة والتحميل
✓ عند كتابة URL، تظهر معلومات الفيديو
✓ يمكن الضغط على "Download"
✓ يبدأ التحميل في الخلفية
```

**الخطوات:**
1. اضغط على "Home" في القائمة السفلية
2. اكتب URL فيديو صحيح (مثل: https://www.youtube.com/watch?v=dQw4w9WgXcQ)
3. اضغط "Preview"
4. تحقق من ظهور معلومات الفيديو
5. اضغط "Download"

### اختبار 2: البحث (Search)
```
✓ يمكن البحث عن الفيديوهات
✓ تظهر نتائج البحث
✓ يمكن تغيير المنصة (YouTube, Instagram, etc)
✓ لا انهيار عند البحث الفارغ
```

**الخطوات:**
1. اضغط على "Search" في القائمة السفلية
2. اكتب كلمة بحث (مثل: music)
3. اضغط Search button
4. تحقق من ظهور النتائج

### اختبار 3: تحويل الصوت (Convert)
```
✓ يمكن إدخال URL
✓ يمكن اختيار جودة وصيغة
✓ يبدأ التحويل عند الضغط
✓ تظهر حالة التحويل
```

**الخطوات:**
1. اضغط على "Convert" في القائمة السفلية
2. اكتب URL فيديو
3. اختر جودة (High/Medium/Low)
4. اختر صيغة صوت (MP3/M4A/WAV/FLAC)
5. اضغط "Convert"

### اختبار 4: التحميلات (Downloads)
```
✓ يظهر قائمة التحميلات الجارية
✓ يظهر قائمة التحميلات المكتملة
✓ تظهر بيانات التقدم
✓ يمكن إيقاف/استئناف التحميلات
```

**الخطوات:**
1. اضغط على "Downloads" في القائمة السفلية
2. تحقق من ظهور التحميلات

### اختبار 5: الإعدادات (Settings)
```
✓ يمكن تغيير الجودة الافتراضية
✓ يمكن تغيير موقع التحفظ
✓ يمكن تفعيل تحويل MP3 تلقائي
✓ يمكن تفعيل الوضع الليلي
```

**الخطوات:**
1. اضغط على "Settings" (الإعدادات)
2. غير جودة التحميل
3. غير موقع التحفظ
4. فعل تحويل MP3
5. فعل الوضع الليلي

---

## 🔍 اختبارات الأخطاء

### اختبار الأخطاء 1: URL غير صحيح
```
1. اكتب URL غير صحيح (مثل: hello world)
2. لا يجب أن يحدث انهيار
3. يجب أن يظهر خطأ مناسب
```

### اختبار الأخطاء 2: لا اتصال بالإنترنت
```
1. اطفئ الإنترنت (قطع الـ WiFi)
2. حاول تحميل فيديو
3. يجب أن يظهر خطأ اتصال
4. لا يجب أن يحدث انهيار
```

### اختبار الأخطاء 3: الخادم معطل
```
1. إذا كان الخادم معطل
2. حاول تحميل فيديو
3. يجب أن يظهر خطأ
4. لا يجب أن يحدث انهيار
```

### اختبار الأخطاء 4: تغيير الشاشة أثناء التحميل
```
1. ابدأ تحميل
2. غير الشاشة (مثل Home إلى Search)
3. لا يجب أن يحدث انهيار
4. يجب أن يستمر التحميل في الخلفية
```

---

## 📊 اختبارات الأداء

### اختبار الذاكرة
```
1. افتح Android Studio
2. اذهب إلى Tools → Profiler
3. ابدأ التطبيق
4. تابع الذاكرة أثناء الاستخدام
5. تحقق من عدم تسرب الذاكرة
```

### اختبار البطارية
```
1. افتح Android Profiler
2. ابدأ تحميل طويل
3. تابع استهلاك البطارية
4. تحقق من عدم الاستهلاك المفرط
```

---

## 📱 اختبار على أجهزة حقيقية

### التحضير
```
1. وصل الجهاز بـ USB
2. فعل "Developer Options" على الجهاز
3. فعل "USB Debugging"
```

### التثبيت والاختبار
```
1. Run → Run 'app'
2. اختر جهازك من القائمة
3. اضغط OK
4. اختبر جميع الميزات على الجهاز الحقيقي
```

---

## 🔐 اختبارات الصلاحيات

### اختبار صلاحيات التخزين
```
1. Run التطبيق
2. افتح Settings → Apps → VidDown → Permissions
3. تحقق من:
   - [ ] Storage (قراءة/كتابة)
   - [ ] Internet
   - [ ] Notifications (Android 13+)
```

### منح الصلاحيات
```
1. عند فتح التطبيق أول مرة
2. قد يطلب الصلاحيات
3. اضغط "Allow" لمنح الصلاحيات
```

---

## 🐛 استكشاف الأخطاء

### الخطأ: "No NavHostFragment found"
```
المشكلة: الـ Fragment container غير صحيح
الحل: 
  1. تأكد من activity_main.xml فيه FragmentContainerView
  2. افعل Clean و Rebuild
```

### الخطأ: "Null Pointer Exception"
```
المشكلة: Fragment أو Activity تم فصله
الحل:
  1. تأكد من جميع الـ Callbacks فيها فحوصات isAdded()
  2. تأكد من getActivity() != null قبل الاستخدام
```

### الخطأ: "Permission Denied"
```
المشكلة: الصلاحيات ناقصة
الحل:
  1. افتح Settings → Apps → VidDown → Permissions
  2. منح جميع الصلاحيات المطلوبة
```

### الخطأ: "Cannot connect to server"
```
المشكلة: الخادم معطل أو URL غير صحيح
الحل:
  1. تحقق من اتصال الإنترنت
  2. تحقق من URL الخادم صحيح
  3. تحقق من أن الخادم يعمل
```

---

## ✅ قائمة الفحص النهائية

### قبل الإصدار
- [ ] جميع الاختبارات تمرت بنجاح
- [ ] لا توجد أخطاء في Logcat
- [ ] لا توجد تحذيرات مهمة
- [ ] التطبيق مستقر ولا ينهار
- [ ] جميع الميزات تعمل بشكل صحيح
- [ ] الصلاحيات كاملة
- [ ] الاتصال بالخادم يعمل

### معلومات النشر
```
- الإصدار: 1.0.0
- رمز الإصدار: 1
- SDK الهدف: 34
- SDK الأدنى: 24
- الحالة: ✅ جاهز للإنتاج
```

---

## 📞 الدعم

للمساعدة أو الإبلاغ عن أخطاء:
1. افتح Android Studio Logcat
2. ابحث عن رسائل الخطأ الحمراء
3. اتبع رسالة الخطأ للعثور على السبب
4. جرب الحلول المقترحة أعلاه

---

**آخر تحديث**: 2024
**الإصدار**: 1.0.0