{
  "name": "viddown-server",
  "version": "1.0.0",
  "description": "Video download server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "install-deps": "npm install",
    "postinstall": "pip install yt-dlp || echo 'Warning: pip install failed, continuing...'"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "fluent-ffmpeg": "^2.1.2",
    "multer": "^1.4.5-lts.1"
  }
}